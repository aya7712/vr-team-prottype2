#!/usr/bin/env sh
# packages/配下の変更を含むコミットにのみ format/lint/typecheck/test を要求する
# (implementation-rules.md 9.1)

if git diff --cached --name-only | grep -q '^packages/'; then
  npm run format:check &&
  npm run lint &&
  npm run typecheck &&
  npm run test
fi
